export default function StaticPage({ title }) {
  return <section className="container section"><h1>{title}</h1><p className="muted">This page is a simple placeholder. Replace it with your final legal copy before production submission.</p></section>;
}
